---

- name: Make sure dist up to date
  apt: upgrade=safe force=yes

- name: Make sure base packages installed
  apt: name={{ item }} state=present update_cache=yes
  with_items:
    - vim
    - curl
    - wget
    - unzip
    - strace
    - iotop
